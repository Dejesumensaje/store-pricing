# Architecture & Product Decisions

A lightweight log of the decisions behind Store Pricing — so the *why* survives.

**Format (one entry per decision):**

```
### ADR-NNNN — Short title
**Status:** Accepted · **Date:** YYYY-MM-DD
**Context:** what problem / constraint prompted this.
**Decision:** what we chose.
**Consequences:** tradeoffs, what it enables, what we gave up.
```

Append a new entry per decision; don't rewrite old ones (supersede with a new ADR and link back). Newest at the bottom.

---

### ADR-0001 — HQ recommendations are proposals, not live in SAP
**Status:** Accepted · **Date:** 2026-06-22
**Context:** The grid felt "disjointed." Root cause: the code modeled HQ-pushed prices as *already live* (`applyHqReview` set `currentBasePrice = recommendedBasePrice`), so current and recommendation were the same number and couldn't be shown as distinct things.
**Decision:** An HQ recommendation is a *proposal*. `currentBasePrice` stays the live price; `recommendedBasePrice` is HQ's proposal; `newBasePrice/newRetailPrice` is the director's decision. The director decides per item: **Accept** (apply the rec), **Override** (own price), or **Keep current** (reject).
**Consequences:** The grid can show current / recommendation / decision as three real things. Margins now compute against the true current price. `hqReviewPending` means "a recommendation awaiting a decision," not "already live."

### ADR-0002 — Accepting a recommendation does not auto-send
**Status:** Accepted · **Date:** 2026-06-22
**Context:** If HQ recs aren't live, what does "Accept" do — send immediately?
**Decision:** Accept (and Override) create a *pending* change that the director then saves for later or adds to a batch and sends to SAP later. Only **Keep current** sends nothing. Rides the existing override → pending → in_batch → submitted pipeline unchanged.
**Consequences:** No new submission machinery; accept/override behave like any edit. The director controls when things go to SAP.

### ADR-0003 — Grid shows current / recommendation / decision as three price columns
**Status:** Accepted · **Date:** 2026-06-22
**Context:** Need to make "what's live, what HQ proposes, what I decided" legible at a glance.
**Decision:** Columns **Current SAP**, **HQ rec** ("—" when none), **Your price**. For temporary allowances, each shows **Base + Retail** on two lines (a TA carries two live prices).
**Consequences:** Information-rich but spatially expensive — this drove later decluttering (ADR-0005, ADR-0007). Considered but rejected: merging into one before→after column (lost the side-by-side clarity the director wanted).

### ADR-0004 — "Change type" label + short pills (TA/EDLP) with tooltip
**Status:** Accepted · **Date:** 2026-06-22
**Context:** "Pricing strategy" was ambiguous ("the current one, or the one I set?"). Long labels also crowded the column.
**Decision:** Rename the column to **Change type** (Neil's suggestion); render values as short DS Badge pills — **TA, EDLP, Base, New, Disc.** — with the full name on hover. Neutral tone (color reserved for Status).
**Consequences:** Unambiguous (a change type is inherently about the action being applied). Short pills keep the column narrow; the tooltip preserves discoverability of the full name.

### ADR-0005 — Merge Decision + Status into one column
**Status:** Accepted · **Date:** 2026-06-22
**Context:** Decision (Pending/Accepted/Overridden/…) and Status (Needs review/Edited/In batch/Pending SAP/Live) sat as two adjacent badges that overlapped (Pending≈Needs review, Changed≈Edited).
**Decision:** One **Status** column: the workflow badge is the primary signal (it's what you scan), with the HQ-relative decision (**Accepted/Overridden/Kept current**) as a quiet gray qualifier *only when it adds info*. Pending and a plain director "Changed" are left off so untouched Live rows stay calm.
**Consequences:** Removes a column and the redundancy. Decision nuance is preserved as a sub-label, not a colored badge.

### ADR-0006 — Multiple changes: badge + ⓘ tooltip, no count
**Status:** Accepted · **Date:** 2026-06-22
**Context:** A single item can change base + retail + fuel saver. We need to signal "several changes → see detail" without noise. A "3 changes" number next to the badge felt cluttered.
**Decision:** When ≥2 fields change, show a small **ⓘ** next to the status badge; hover reveals the full itemized breakdown. No count surfaced.
**Consequences:** Clean at-a-glance, detail on demand. Known tradeoff: hover detail is hidden on touch / not keyboard-reachable — fine for the desktop testing target; revisit for touch.

### ADR-0007 — Selection limited to items with a decision; no bulk editing
**Status:** Accepted · **Date:** 2026-06-22
**Context:** Users could select rows with no change, which did nothing (a dead-end). An earlier bulk-edit feature was built and then fully reverted as "wrong direction."
**Decision:** Only items with a pending decision are selectable (checkbox disabled otherwise; select-all picks only decided rows). **No bulk editing** — refine existing interactions instead of adding capabilities.
**Consequences:** The multi-select → batch flow is unambiguous. Pricing stays one-item-at-a-time via the drawer (matches how the team wants it).

### ADR-0008 — Drawer: decision up, context collapsed
**Status:** Accepted · **Date:** 2026-06-22
**Context:** The drawer stacked ~7 bordered cards; the price decision competed with margin/competitor/related/impact.
**Decision:** Cost + allowance cost move to the item header (reference info). Three explicit sections — **Base price / Retail price / Fuel saver**. Margin is inline feedback next to each price. Competitor / related / impact collapse into accordions. Labels unified to **Current SAP / HQ recommended / Your price**. Actions: Accept / Override / Keep current.
**Consequences:** The decision stays above the fold; context is one click away. Less scroll, clearer hierarchy.

### ADR-0009 — Filters: searchable accordion facets, built to scale
**Status:** Accepted · **Date:** 2026-06-22
**Context:** Flat checkbox lists don't scale to hundreds of categories/brands; active filters were invisible.
**Decision:** Rebuild `FilterDrawer` as **accordion facets** (collapsed by default, except active ones) with a **per-facet search** (appears past ~8 options), selected-first ordering, a capped list with **"Show all (N)"**, a per-facet count badge, and a **Clear** action in the facet header next to the label/pill. Use the persistent DS `Input` (not the collapsible `SearchInput`).
**Consequences:** Scales to hundreds of values per facet without virtualization. If catalogs reach thousands of *rows*, the table itself will need virtualization (still open).

### ADR-0010 — Seed a realistic catalog for filter scale
**Status:** Accepted · **Date:** 2026-06-23
**Context:** The mock had one category ("Potato chips") and ~6 brands, so the scalable filters had nothing to scale against.
**Decision:** Recategorize the hand-crafted demo items to "Snacks" (coherent) and add a deterministic synthetic catalog (~20 categories, ~70 subcategories, ~36 brands, all "Live") — without touching the demo items that drive flows (IDs, line groups, overrides, HQ recs).
**Consequences:** Filters now exercise real scale and the Category column is meaningful. `TOTAL_ITEM_COUNT` reflects the real count. Deterministic generation avoids hydration mismatches.

### ADR-0011 — Minimal default table: one adaptive Price column, granular prices opt-in
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Usability review (Claire): the table showed too many columns by default — **four** price-ish columns (Change type / Current SAP / HQ rec / Your price), each stacking base+retail lines for a TA, drowned the item name. First cut over-corrected to *zero* price columns, which turned the decision queue into a phone book: Status said a decision was *owed* but gave no sense of *what's at stake* (a 2¢ nudge vs a 30% cut), so triaging hundreds of recs meant opening every drawer.
**Decision:** Default columns are **select · ID · item (pic+name) · Category · Price · Status**. The IA fix is to show the *delta*, not the *components*: a single adaptive **Price** column (`PriceMoveCell` + `priceMove()`) renders `current → target` with a ↓/↑ % move — target = the director's decision if any, else HQ's pending proposal, else just the muted current price (nothing moving). Multi-unit deals show "N for $X" with a per-unit delta; sortable by **magnitude of change** so a click surfaces the biggest movers. Direction is carried by the ↓/↑ glyph (not loaded red/green, since "good" is ambiguous between shopper savings and margin). The granular **Change type / Current SAP / HQ rec / New price** columns move into the opt-in `ColumnsMenu` (leading the list) for auditing exact numbers; "Your price" → **New price**. Store table is `flat` (no splitPane), so the column set is free to change.
**Consequences:** Clean scan *and* triage signal in one column. Mobile card drops the 3-up price triad for the same single `PriceMoveCell` line. `CurrentSapCell`/`HqRecCell`/`YourPriceCell` live on as the opt-in column cells.

### ADR-0013 — Speak the shelf: change type as a physical-tag layer, not hidden metadata
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Hiding "Change type" (ADR-0011) removed the layer a director actually prioritizes on. At Hy-Vee the change type *is* a physical reality on the shelf: **base = white tag** (regular), **temporary allowance = yellow promo tag** ("SAVINGS THIS WEEK", time-bound), **EDLP = permanent reduction on the white shelf tag**, **new = a fresh tag**, **discontinued = clearance/markdown**. The push (Neil/team): don't build a *standard* table — make it look like how prices appear in the physical store, so "what am I editing / what do I prioritize" is visible.
**Decision:** Reintroduce change type as a first-class, store-grounded **Tag** column (default, replacing the old neutral "Change type" badge). `shelfTagKind()` maps each item to a `ShelfTagKind` (yellow / white / edlp / new / clearance); `SHELF_TAG_META` carries the swatch + label + price-tint. `ShelfTagCell` renders a colored tag swatch + label; the **Price target is tinted to its tag** (amber for promos, rose for clearance, emerald for new — white/EDLP stay plain) so the Price column previews the shelf and "a wall of yellow" = this week's promos. A **yellow tag also surfaces its end date** ("· ends Jun 28") since temporary allowances are the time-sensitive, prioritize-first work. New items render "Set $X" (no current → arrow). Default column order: select · ID · item · Category · **Tag** · Price · Status. Mobile card swaps its strategy Badge for `ShelfTagCell`. Direction stays the ↓/↑ glyph; tag color is the *type* lens, not a good/bad signal.
**Consequences:** The table reads like the aisle and restores prioritization by tag family (yellow promos first). Color now encodes shelf-tag *type* (semantic, store-true) while the Status badge keeps workflow state — two distinct, non-competing color systems. Open: EDLP shares the white swatch (white family) with only its label to distinguish; revisit if directors need EDLP to pop more. Grouping-by-tag (the segmented option) deferred — per-row tint covered the need without restructuring the table.

### ADR-0014 — Drawer = designing the shelf tag: live "what the shopper sees" preview
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Extending the shelf metaphor (ADR-0013) from the table (the aisle seen from afar) into the drawer (the tag in your hand). The drawer was a form; a director is really deciding which tag to hang and what the customer will see. Abstract price inputs don't let them validate the decision in physical terms.
**Decision:** New `ShelfTagPreview` pinned under the item identity at the top of `ItemEditDrawer`, rendering the actual shelf tag(s) this edit produces, updating live (reads the store-driven `item`): a **white tag** (regular/permanent price, strikes "was $X" when base/EDLP changes) and, for a temporary allowance, the **yellow promo tag** — "SAVINGS THIS WEEK", the deal ("N for $X" or single), **Save** off the regular price (cents under $1, Hy-Vee style), the allowance window, and a blue **Fuel Saver** chip when active. The yellow tag dims with a dashed border + "proposed" pill when previewing HQ's rec before the director decides. New items → "New item" kicker + "Set" price; discontinued → "Clearance". Savings are computed off the **base/white-tag** price (consistent with ADR-0012's reduction reference). `shelfTagKind`/`fmtShortDate` exported from `buildStoreColumns` for reuse. Existing per-section captions ("· yellow promo tag" / "· white shelf tag") stay as the input-side anchor.
**Consequences:** The drawer now reads as "design the tag, see what the shopper sees," making the consequence of an edit tangible and validating it physically before send. Verified live (Playwright): single promo (Lay's $3.49, SAVE 80¢), multi-unit + base-change + fuel (Doritos 3/$12.00, SAVE $5.37, was $5.49, +$0.10 fuel). App-wide arc noted (batch tray = a sheet of tags to print; send = update shelf tags) but not built this round.

### ADR-0015 — White tag crossed out + drawer IA cleanup (demote the noise)
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Neil's shelf photo shows the white tag **X'd out** when a yellow promo is the active price. And an IA review of the drawer found redundancy/noise competing with the decision: the proposed price `$2.50` appeared 3× (preview, reference grid, accept button); a titled, non-actionable locked "Price type" block; a generic HQ note ("accept it, enter your own, or keep") that the accept-first buttons already imply; and inline margin rows (analyst detail). Sarah: the director should *understand what's being recommended*, not just see a price.
**Decision:** (1) **White tag gets a red X** in `ShelfTagPreview` when a yellow promo is present (`crossed` prop, SVG diagonal). (2) Four IA demotions in `ItemEditDrawer`, all confirmed by the user: **remove the per-section price reference grids** (Current SAP / HQ rec) in both retail + base — the shelf preview already shows crossed white + yellow; **collapse the locked "Price type" block to a one-line caption** ("🔒 Temporary allowance · price & dates editable, type set by HQ"); **replace the generic HQ note with the rationale** (`hqRecRationale()` → "HQ proposes a vendor-funded promo · save 69¢ · runs Jun 24–30" for TA, "HQ recommends lowering the price to $X (from $Y)" otherwise); **move margin rows into the "Projected impact" accordion** (consolidated financials). (3) Seeded a clean TA **proposal** in the HQ queue (mock `RBCS5-9` Goldfish, added to `HQ_REVIEW_IDS`) to exercise the accept-first "what HQ recommends" flow + the "proposed" preview state.
**Consequences:** The drawer is dramatically calmer — decision (Accept $X / Set a different price) up top, the shelf preview carries the prices, context explains the *why*. `$2.50` now appears 2× doing two different jobs (result vs action) instead of 3×. Verified live (Playwright) on the HQ proposal (Goldfish) and an edit-mode TA (Doritos).

### ADR-0016 — "Tags to hang": the send flow as a pile you build and clear (kill the teleport)
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Usability test on batch sending surfaced three things: (1) people love the *feeling of clearing/resolving* — the empty HQ tab felt like "mission accomplished"; (2) the "To send" screen was badly disconnected — Sarah circled, never found the batches, repeatedly tried to reach them *from the drawer*; (3) the root cause: the mental model is "I'm editing things," but swapping context to a separate batch screen made the edits *disappear* — they couldn't find where their changes went. Extending the shelf metaphor: editing = **printing a tag**; the edited items = a **pile of tags to hang**; sending = **hanging them on the shelf**; the empty pile = the satisfying release.
**Decision:** Replace the full-screen `view="batch"` teleport (primary path) with an **in-context sheet** (`TagsToHangSheet`, a DS Drawer that slides *over* the items table — same context). The "To send" button → **"Tags to hang"** (icon `Tags`), always visible with a live count, so edits never feel lost. The sheet lists the pending pile as **tag-styled rows** (shelf-tag swatch, Base/Retail, current → tinted new price), **yellow promos first**, each removable. Primary action **"Hang all N on the shelf"** → `sendAllPending` → success toast + the sheet flips to a **celebration empty state** ("Shelf's all caught up · every tag you printed is hung"). An "Or organize into batches to schedule" link still opens the full `BatchTrayView` (advanced; not dead). **Drawer footer reworked**: removed the confusing in-drawer batch assignment (`BatchSplitButton` / "Add to batch" — what Sarah fought) in favor of a clear pile connection — **"🏷 In Tags to hang · Review"** (new `onReviewTags` prop closes the drawer + opens the sheet) next to Next item / Done. Dropped the drawer's `activeBatch`/`openBatches`/`onAddToBatch`/`onNewBatch` props.
**Consequences:** Edits accumulate in a visible pile, reviewed in-context, cleared with a satisfying "done" — the three findings addressed in one move. Named batches/scheduling survive as an opt-in advanced path, not the default. Verified live (Playwright): pile sheet over the table (5 tags), hang-all → "5 tags hung" toast + celebration, drawer footer "In Tags to hang · Review". `SHELF_TAG_META` exported for the sheet's swatches. The bulk ActionBar still uses the batch helpers (unchanged). **Superseded in part by ADR-0017** (the separate sheet became a lens on All items).

### ADR-0017 — One home: the tag lifecycle; "Tags to hang" is a lens on All items, not a destination
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Holistic rethink after more feedback. Consistency problems: three competing concepts (HQ queue, the Tags-to-hang pile, batches) and two surfaces deciding "what happens to my edit" (drawer + separate page). User's framing: the mental model is "I'm editing"; a separate destination makes edits *disappear*. Also: the HQ tab must drain and **stay empty** after sending (decided items live in All items); recently-touched items should rise to the top of All items. The metaphor gives a clean lifecycle.
**Decision:** One lifecycle, **one home (All items = the shelf)**: **Needs review** (HQ proposal) → **Ready to hang** (decided/"printed") → **Hanging** (sent) → **Live** (confirmed); `in_batch` = **In a tag run** (`item-status.ts` relabeled, tones kept). **"Tags to hang" is now a LENS over All items**, not a separate sheet: the header button toggles `hangLensOn`, which filters the same table to the ready tags and shows a **Hang-all bar** ("N tags are printed and ready · Hang all N on the shelf") + a "Schedule a tag run instead" link (→ the advanced batch workspace); clearing flips to the in-place celebration ("Shelf's all caught up"). `TagsToHangSheet` retired as the destination. **Recent-first**: added `Override.updatedAt` (set in `upsertOverride`); All items sorts items with active edits to the top, newest first. The button count + lens + bar all use the **item** count (`pendingItemIds.size`), not the override count, for consistency. Drawer footer connection relabeled to **"Ready to hang · View tags"** (`onReviewTags` now opens the lens). Decisions confirmed with the user: lens-over-All-items (vs. separate sheet), and HQ accept routing kept **optional & inline**.
**Consequences:** One surface, one mental model — edits never teleport; the director sees their work in the shelf they already know, recent on top, and clears it in place. HQ already drains permanently (`hqReviewNeeded` excludes decided items); decided items now surface recent-first in All items. Verified live (Playwright): All items lifecycle statuses, lens filter + Hang-all (4 items) → "4 tags hung" + in-place celebration. **Superseded in part by ADR-0018** (HQ tab → guided flow).

### ADR-0018 — HQ review is a guided flow, not a tab; All items is the recommendation-free home
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Simulating the whole journey (screenshots) exposed real incoherence — the build had been patched, not designed. Concretely: the HQ banner still said "HQ set these prices and they're **already live**" (contradicting the proposal/accept/hang model); **two** "Accept" buttons (inline + footer); the drawer titled "Edit prices" while *reviewing*; accepting didn't auto-advance (no drain); the journey was fragmented across four disconnected surfaces (All tab, HQ tab, Tags-to-hang lens, batch view). Root IA error: the pipeline stages (Proposal → Decide → Ready → Hung) were presented as peer tabs, not one flow.
**Decision:** Two real things, not four. **All items = the shelf**: the director's permanent, *recommendation-free* workspace to find any item, see its live state, and edit prices on their own terms (ad-hoc edits → "Ready to hang" → hang via the lens). The **MainTabs (All/HQ) are removed**; All items is just the home, titled with its count. **HQ proposals are a guided flow** (`ReviewFlow`, new): a focused, one-proposal-at-a-time experience (`reviewMode` swaps the main content) — shelf preview + rationale + **Skip / Tweak / Accept**, each **auto-advancing**, with a progress bar and running "N ready to hang" tally; at the end, a summary → **"Hang all N on the shelf"** → toast + celebration. One continuous motion: review → decide → drain → hang. Entered from a clear CTA on All items ("HQ sent N recommendations to review — Review →", pulsing dot) and the header bell. The stale "already live" `HqReviewBanner` is gone; `hqRecRationale` extracted to `src/lib/hq-rec.ts` (shared by the drawer + ReviewFlow); `TagsToHangSheet` deleted (superseded by the lens). `activeTab`/`MainTab` removed; drawer `flow` fixed to "all".
**Consequences:** The two top jobs are now coherent and separate: **"review what HQ sent"** and **"manage my prices"** (All items, no recs in the way). **Review interaction refined in ADR-0019** (the 1-by-1 wizard → a scannable worklist).

### ADR-0019 — Review is a scannable worklist (bulk + dive-in), not a 1-by-1 wizard; HQ stays drained after send
**Status:** Accepted · **Date:** 2026-06-23
**Context:** The guided 1-by-1 card (ADR-0018) felt good but was **too extreme**: HQ can send ~100 recs, and a forced linear march is exhausting and **strips decision context** (you can't see neighbors, patterns, or decide in bulk). Also surfaced a real bug the user predicted: after **hanging** (send → override goes `submitted`), `hqReviewNeeded`'s `!hasOverride` flipped back true and accepted recs **re-appeared in the queue**.
**Decision:** `ReviewFlow` rebuilt as a **worklist**: all recs in a scannable list (context + patterns), each row = item + rationale + `current → proposed` (tinted to its tag) + %/flag, with inline **Accept ✓ / Skip ✕ / open ›**. **Bulk**: select-many → "Accept selected (N)" + "Accept all N". **Sort**: Needs attention (flagged — alerts or ≥15% swing — first, then magnitude) / Tag / Item. **Dive-in** (`onOpenItem` → the drawer) gives the full card only for the ones that need a closer look; the 1-by-1 focus is now opt-in, not forced. The list **drains live** (rows leave as decided) → ends in the same summary → "Hang all N" → celebration. **Bug fix**: deciding on an HQ rec (accept/override via `updateBasePrice`/`updateRetailPrice`) now sets `reviewed: true`, so it leaves the queue *permanently* and never returns after the override goes submitted. Seeded 8 extra HQ proposals (`HQ-101…108`, mix of routine + flagged) so the worklist shows real scale.
**Consequences:** Review now scales (bulk-accept the routine, dive into the flagged few) while keeping context, and still delivers the drain→send payoff. After send, HQ stays empty; decided items show their state in All items. Verified live (Playwright). **Metaphor copy + batch model reworked in ADR-0021.**

### ADR-0021 — Dial back the metaphor copy; batches = inline scheduled sends (no workshop)
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Two honest user notes: (1) the shelf metaphor was **over-applied to workflow copy** ("Tags to hang / hang on the shelf / printed / tag run") — cute, not clear; (2) **batches still felt clunky** — a heavyweight parallel system (separate `BatchTrayView` workspace + `NewBatchModal`), and the "include in a batch?" decision was disconnected from where it makes sense.
**Decision:** **(a) Language dial-back** — the metaphor stays where it's *real* (the drawer's white/yellow "what the shopper sees" tag preview, base=white tag / retail=yellow tag), but the workflow speaks plainly: **"Tags to hang" → "To send"**, **"Hang all on the shelf" → "Send all to SAP"**, **"printed/ready to hang" → "ready to send"**; statuses **Ready / Sending / Scheduled / Live** (`item-status.ts`); celebration "All caught up / All sent". **(b) Batch = a scheduled send, inline** — there is no separate workshop or create-batch modal in the primary flow. On the **To send** surface you either **Send all to SAP** now, or **select rows → Schedule… → pick a date** (small Modal + `DateField`) which groups them into a **scheduled batch** shown inline ("Scheduled · Jun 24 · 3 items · Send now"). The per-item "batch or not" decision is gone — you approve into "To send", then group/schedule there with full context. Implemented in `page.tsx`: `sendSelectedNow` (createBatch+submitBatch), `scheduleSelected` (createBatch+scheduleBatch), the ActionBar now offers **Send now / Schedule…**, and a Scheduled section lists scheduled groups. Removed the `BatchSplitButton` from the bulk bar + the dead `addOverridesToBatch`/`handleBulkAddToBatch`. Counts standardized to **items** (not override records) across the surface.
**Consequences:** Clearer copy; batching is now a lightweight, contextual action instead of a parallel system. **Batch model corrected/expanded in ADR-0022** (batches are central, not an afterthought).

### ADR-0022 — Batches are the central object: an "Unassigned" inbox you sort into multiple scheduled batches
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Key domain correction from the user: in their real system **batches are a central control mechanism**, not an optional scheduled send. A store runs **multiple active batches concurrently, each scheduled independently** — because sending *all* a store's changes to SAP at once would overload printing/control. So the director has **two conscious decisions**: (a) the price (accept/edit), and (b) **which batch + when** (throughput management). ADR-0021 under-modeled (b) as a hidden afterthought. Batches must be **evident and central**, and assignment must be a deliberate, scalable act — drawer included.
**Decision:** The **To send** surface is now batch-centric, pattern = **Unassigned inbox + a list of your batches** (the chosen IA pattern over kanban / batch-builder). **"Your batches"** (the control panel) lists every draft/scheduled batch with name, status (Draft/Scheduled), schedule date, item count, and per-batch **Schedule** (Modal+`DateField` → `scheduleBatch`) / **Send now** (`submitBatch`) / open-to-**manage** (`BatchDetailDrawer`), plus **+ New batch** (`NewBatchModal`). **"Unassigned (N)"** is the inbox of changes not yet in a batch (pending overrides); select rows → **Add to batch ▾** (`BatchSplitButton`: existing batch or new) — they move into the batch (in_batch → leave Unassigned, batch count grows), Undoable. A de-emphasized "Send all now / Send now" remains for the odd urgent change (batches are the controlled default). **Per-item assignment is back in the drawer footer** ("Add to batch ▾" via `BatchSplitButton`, restored `openBatches`/`activeBatch`/`onAddToBatch`/`onNewBatch` props) so the batch decision is available both in bulk and per-item. Shared `addOverridesToBatch(batchId, ids)` powers both. Mock seeds two concurrent batches (Tuesday ad prep / Friday endcap reset) to show the multi-batch reality.
**Consequences:** Batches are now the evident, central organizing structure with independent schedules — matching how throughput is actually controlled. Verified live (Playwright): To-send shows 2 batches + Unassigned; select 2 → "Add to Friday endcap reset" → batch 1→4 items, Unassigned shrinks, Undo toast; per-batch "Schedule this batch" modal; drawer footer "Add to batch ▾". Open/next: the old `BatchTrayView` workshop (`view==="batch"`) is fully superseded and **orphaned/unreachable** — delete in a cleanup pass; no sent/confirmed batch *history* view yet (items show Sending/Live in All items). **Review→batch handoff fixed in ADR-0023.**

### ADR-0023 — Connect Review → To send → batches (the handoff seam)
**Status:** Accepted · **Date:** 2026-06-23
**Context:** The Review worklist was disconnected from the batch model. Accepting said "ready to send" but gave **no option to add to a batch**, **no indication the changes fed the To-send queue**, and its end-action **"Send all to SAP" bypassed batches entirely** (you'd send without ever seeing your other batches — the anti-pattern). Plus the success toast (top-right) **covered the "To send" button**.
**Decision:** Make Review *feed* the batch workflow instead of routing around it. **(1)** Removed the review's "Send all to SAP" end-action; the summary now reads "N changes are now in To send. Sort them into batches…" with a primary **"Go to To send"** (`onGoToSend` → exits review, opens the To-send surface where batches live) + "Back to all items". **(2)** Evidence it feeds the queue: a header **"✓ N in To send"** chip (links to To send) + the accept toast "Added to To send" + the live header "To send (N)" count. **(3)** The batch option *during* review: the bulk bar gained **"Add to batch ▾"** (`BatchSplitButton`) — `acceptSelectedToBatch` accepts the selected recs AND drops their overrides into a batch in one step (for directors who sort as they review). `ReviewFlow` gained props `onGoToSend`/`openBatches`/`activeBatch`/`onAddToBatch`/`onNewBatch`; dropped its own `sendAllPending`/hung/celebration. **(4)** Moved `ToastProvider` to **bottom-left** so toasts never cover the To-send button (top-right) or the bulk ActionBar (bottom-center).
**Consequences:** The pipeline is now continuous — Review decides prices → changes land in To send (visibly) → you sort into batches there (or during review). No send that skips the batch view. Verified live (Playwright): bulk "Add to batch" in review, "N in To send" header chip, end "Go to To send", toast at bottom-left clear of the button.

### ADR-0012 — Temporary-allowance drawer: accept-first, pick-the-discount-kind-first, optional base
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Usability test (Sarah + Neil/HQ). Every retail-price input (% off, $ off, exact, multi-unit deal) was visible at once, so the decision wasn't conscious. Mental model clarified: **base price = white shelf tag** (regular), **retail price = yellow tag** (the promo). Sarah wanted to lean toward accepting the recommendation with the option to change — not all options up front. Percent-off was computed off current retail and read as confusing; it should be off the **base** price ($4.49 × 0.9 ≈ $4.04).
**Decision:** In the TA drawer the **retail (yellow-tag) decision is primary** and **accept-first**: when HQ has a rec it opens on `Accept $X.XX` + `Set a different price`. Choosing to change reveals a single **reduction-method chooser** — `% off · $ off · Multi-unit · Exact price` (kind first, number last) — mounting **one input at a time** (`RetailReductionField`). `% / $ off` now reference the **base/white-tag** price with inline math ("10% off $4.49 (base) = $4.04"). The **base price section is optional** in TA (a Switch like fuel saver), default collapsed. Captions name the tags ("yellow promo tag" / "white shelf tag"); the recommendation renders **grayed** as a suggestion. Labels: **New base price / New retail price** (was "Your price").
**Consequences:** Progressive disclosure makes the discount type a conscious choice. `ReductionInput` gained a controlled `mode`/`hideToggle` so the chooser owns $/%. Known tradeoff: switching method after committing can briefly re-shape an existing deal (e.g. exact→multi); fine for the test. Batches flow intentionally untouched this round.

### ADR-0024 — Polish pass: To-send reachable from review, no drawer item-hopping, louder send feedback, batch landing flash
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Four UX refinements from the user after living with the ADR-0023 pipeline. (1) The "To send" button shows during HQ review but clicking it only toggled the `hangLensOn` lens *under* the still-active `reviewMode`, so nothing happened — you couldn't jump to To send mid-review. (2) The drawer's prev/next item navigation (header `‹ X / Y ›` + footer "Next item") made the editing flow feel like a forced march; the user wanted to just finish an item and close back to the underlying flow, in both All items and HQ review. (3) The "To send" count increment was easy to miss (only a small badge pop). (4) Adding items to a batch on the To-send surface gave a toast but no spatial cue of *where* the items landed.
**Decision:** (1) The To-send button's `onClick` now special-cases review: `if (reviewMode) { setReviewMode(false); setHangLensOn(true) }` → exits review and opens the batch-centric To-send surface. (2) **Removed the drawer's item-to-item navigation** entirely — dropped the `onPrev`/`onNext`/`position` props, the header `‹ X / Y ›` control, and the "Next/Next item" footer label; `advance()` is now just `onClose()`, footer reads **"Done"**. Page no longer computes `rowIds`/`drawerIdx`/`onPrev`/`onNext`. Finishing an edit closes the drawer and returns to All items or the review worklist. (3) **Louder increment feedback**: enhanced the `badge-pop` keyframe (bigger overshoot + settle) and added a `send-bump` keyframe that bounces the *whole* To-send button (re-keyed by the existing `trayPulse` counter). (4) **Batch landing flash**: new `batch-flash` keyframe (brand-tinted bg + ring fading out over 1.5s); `addOverridesToBatch` and new-batch creation set `batchFlash {id, n}`, and the matching batch row re-keys while flashing so the highlight restarts even when the same batch is targeted twice. Also gave the review worklist's per-row **Accept/Skip** a clearer hover (Accept scales up; Skip scales + turns red to telegraph "reject"). All animations honor `prefers-reduced-motion`.
**Consequences:** Review → To send is reachable in one click; the drawer is a focused single-item editor (no hopping); increments and batch assignments are visually obvious. Verified live (Playwright): To-send from review leaves review and shows "Your batches"; drawer has 0 nav controls + a single "Done"; adding to a batch flashes its row (brand ring + tint) with the Undo toast. `tsc` clean; eslint at pre-existing baseline (set-state-in-effect / no-unused-expressions only).

### ADR-0025 — To-send gets the lifecycle tabs back (Pending/Scheduled/Sent); drop "Send all now"; sends always go through a batch
**Status:** Accepted · **Date:** 2026-06-23
**Context:** Follow-up on the ADR-0022/0024 To-send surface. (a) The "Unassigned (N) … Send all now" row used a non-DS text-link and offered a batch-less send (`sendAllPending`) — contradicting the batch-as-throughput-control model (ADR-0022). (b) The user missed the old `BatchTrayView` **Pending / Scheduled / Sent** lifecycle tabs and asked to bring them back. (c) A React duplicate-key warning: the To-send button's `send-bump` wrapper and its badge both used `key={trayPulse}` as siblings. (d) The review worklist's icon-only Accept/Skip wanted **tooltips** (the hover scale/▱ animation stays, but the user actually meant labels).
**Decision:** (a) **Removed the entire "Unassigned … Send all now" row** and the `hangAll`/`sendAllPending` path. The only bulk-send is the ActionBar **Send now** (`sendSelectedNow`), which **auto-creates a batch** ("Sent · {date}") and submits it — so *every* send is a batch (satisfying "if I send items without a batch, create one automatically"). (b) The To-send lens header now carries a DS **`ToggleGroup`** (`toSendSegment` state) — **Pending** (drafts + the loose-changes items table = the unbatched inbox) / **Scheduled** (scheduled batches) / **Sent** (submitted+confirmed, read-only rows with a Sending→Live badge + Preview). Batch rows extracted to `renderOpenBatchRow` (draft/scheduled: Schedule/Reschedule + Send now, flashes on landing) and `renderSentBatchRow`. The shared items table renders only for All items or the Pending tab with loose changes; the bulk ActionBar is likewise gated to the Pending tab. (c) Gave the two sibling spans distinct keys (`bump-${trayPulse}` / `badge-${trayPulse}`). (d) Wrapped each worklist Accept/Skip/open button in a DS **`Tooltip`** ("Accept HQ rec" / "Skip — keep current price" / "Open details").
**Consequences:** The To-send surface mirrors the batch tray's lifecycle without a separate `view==="batch"` workshop, batches are the sole send conduit, and the duplicate-key warning is gone. Verified live (Playwright `/tmp/pw-driver/tosend2.mjs`): no console errors, no "Send all now", Pending/Scheduled/Sent tabs render with counts, a sent batch appears under Sent as "Sending" with Preview. `tsc` clean; eslint at pre-existing baseline.

### ADR-0026 — Conscious-edit drawer, batch-prompt modal, "Ready to send" + Sending spinner, lens cleanup
**Status:** Accepted · **Date:** 2026-06-24
**Context:** Final polish on the decide-then-send loop, from a walkthrough. (a) The drawer showed an editable price input up front for every item — even when accepting an HQ rec or viewing an unchanged item, which invited accidental edits and buried the actual decision. (b) The footer's "Add to batch ▾" split-button was confusing as a last step. (c) Status copy: "Ready" was ambiguous; "Sending" gave no sense of being in-flight. (d) On the To-send surface the top-right "To send" button was redundant (the lens header already owns that nav), and the loose-changes table had no heading after the "Unassigned … Send all now" row was removed (ADR-0025). (e) The send-bump animation landed well — opportunity for more of the same to register decisions.
**Decision:** **(a) Conscious-edit drawer** — the editable input only appears after a deliberate choice. Non-TA base section now renders one of: a read-only **"Current price $X" + "Change price"** (unchanged items), an **Accept $rec / Set a different price / Keep current** block (undecided HQ rec, NO input), a **decided summary** ("✓ $old → $new", `decision-pop`) once committed, or the input (after Change / for new-discontinued which must set a price). TA retail gained the same read-only "Current + Set promo price" fallback. The shared base input is extracted to a `baseInputBlock()` helper. **(b) Batch-prompt modal** — the footer is now just **Done**; with a pending change it opens a small Modal ("Add this change to a batch?") listing existing batches (active flagged) + **Create a new batch** + **Decide later in To send**. Removed the footer `BatchSplitButton` and the `onReviewTags` prop. **(c)** `item-status.ts`: "Ready" → **"Ready to send"**; `sent` gains `loading:true` → `StatusCell` + sent-batch rows render a subtle `Loader2` spinner (`animate-spin`, `motion-reduce:animate-none`). **(d)** The top-right To-send button is hidden when `hangLensOn`; added a **"Not in a batch yet"** heading above the loose-changes table. **(e)** New `decision-pop` keyframe (globals.css, scale+fade, reduced-motion gated) on committed-price summaries + the accept-first block.
**Consequences:** Editing is a deliberate act; HQ acceptance no longer dumps the user into an input; the batch decision is an explicit, roomy step; status reads clearer with an in-flight cue. Verified live (Playwright, fresh dev server): unchanged item shows read-only + "Change price" (0 inputs → 1 after click); HQ item shows Accept/Set/Keep with 0 inputs; Done opens the 3-option batch modal; lens shows 0 top-right To-send buttons + "Not in a batch yet" + "Ready to send" statuses; no console errors. `tsc` clean; eslint at pre-existing baseline (set-state-in-effect only).

### ADR-0027 — Polish pass (two audit subagents): safer sends, recoverable review, status + typography consistency
**Status:** Accepted · **Date:** 2026-06-24
**Context:** After merging the conscious-edit drawer (ADR-0026) we ran two read-only audit subagents — a typography/visual-consistency pass and a UX/usability/flow pass — over the rendered screens + source. Highest-value, high-confidence findings were applied; product-shaping or speculative ones were deferred.
**Decision:** **Flow/safety:** (1) An `in_batch` item was labeled **"Scheduled"** even when its batch was a Draft (no send date) — relabeled **"In a batch"** (`item-status.ts`); "Scheduled" now means a real scheduled batch only. (2) Removed the bulk **"Send now"** from the ActionBar (and `sendSelectedNow`) — it sent straight to SAP with no Undo, defeating the batch-dosing premise; batches are now the sole send path (per-batch Send now remains). (3) **Review is recoverable:** per-row Accept (`acceptRow`) + Skip + bulk Accept now toast with **Undo** (`undoAccept` = drop the pending override + un-review); **"Accept all"** confirms when the set includes flagged proposals (big swing / alert) via `ConfirmDialog`. (4) Review's **"Skip" → "Keep current price (reject)"** (tooltip + aria) — "Skip" wrongly implied defer; it permanently rejects. (5) Drawer batch-prompt: **"Decide later" is now a real tertiary Button** ("Leave it — sort later in To send"), not a weak text link. **Typography:** added missing `tabular-nums` (EDLP savings, HQ-recommended caption); `CollapsibleSection` title `font-medium`→`font-semibold` to match other section heads; "Line price"/"Line" attribute badges `in-progress`→`neutral` (reserve `in-progress` for workflow status); unified the two "all done" empty-state icons to `size-12`.
**Consequences:** No irreversible one-tap SAP send; accidental review taps are Undoable in place; status wording no longer lies about scheduling; clearer type hierarchy. Deferred (logged, not done): full muted-gray token sweep; unifying the three batch-pickers into one component; renaming the "Pending/Ready to send/Not in a batch yet" trio; the dev-only "1 issue" indicator (Next.js, won't ship). Verified live (Playwright, fresh server): accept-all confirm shows "6 flagged…", bulk bar has 0 "Send now", "In a batch" status renders, no console errors. `tsc` clean; eslint at baseline.

### ADR-0028 — EDLP markdown chooser, one unified batch-picker, status-word consistency
**Status:** Accepted · **Date:** 2026-06-24
**Context:** Tackling the deferred items from ADR-0027 plus a new request: EDLP price edits should let the director choose HOW to apply the markdown (like a temporary allowance), instead of showing a $/%-toggle reduction input AND an exact-price input at once.
**Decision:** (1) **`BaseReductionField`** (new) — a segmented **% off / $ off / Exact price** chooser (one input mounted at a time, % / $ computed off the current base price), mirroring TA's `RetailReductionField` minus Multi-unit. The drawer's `baseInputBlock` uses it for EDLP; plain base/new-discontinued keep the single exact `PriceInputCell`. `ReductionInput` is no longer used directly in the drawer. (2) **`BatchPickerModal`** (new, shared) — extracted from the drawer's batch prompt and now the single batch-assignment surface for all three entry points: the drawer's Done, the bulk **ActionBar** (replaced `BatchSplitButton` with an "Add to batch" button that opens it), and the **Review** worklist bulk bar ("Accept & batch" → opens it; accepts the selection and drops it into the chosen batch). Presentational: emits the choice, caller assigns + closes; optional `onLater` escape (drawer only). (3) **Status-word consistency:** the loose-changes section heading "Not in a batch yet" → **"Ready to send" (N)** so it matches the per-row status badge; "not in a batch yet" demoted to subtext. (4) **Drawer standardization:** revert (`RotateCcw`) now always lives in the `Field` top-right action slot for base/EDLP (matching retail), not crowding the input row. (5) Validated on mobile (390px): drawer is full-width, both reduction choosers wrap cleanly, the shared picker renders as a centered dialog.
**Consequences:** EDLP and TA price decisions now feel identical (pick the method, then the number); "add to a batch" looks and reads the same everywhere; the ready-but-unbatched state has one name. `BatchSplitButton` is now unused by these surfaces. Verified live (Playwright, desktop + mobile, fresh server): EDLP shows 3 method buttons, all three pickers show the same modal, "Ready to send" reads consistently, no console errors. `tsc` clean; eslint at pre-existing baseline (DataTable/FilterDrawer/NewBatchModal rules-of-hooks are pre-existing, not from this change).

### ADR-0029 — Muted-gray pass (confirmatory) + store-director walkthrough fixes
**Status:** Accepted · **Date:** 2026-06-24
**Context:** Two things: (a) the deferred muted-gray token sweep, and (b) a walkthrough of the whole flow from the store director's seat (driving the real app as the daily user, not the designer), to surface friction that only shows up in use.
**Decision (gray pass):** Inventoried `text-gray-*` across every key surface. Finding: usage is already role-consistent — gray-900 primary, gray-700 labels, gray-600 body/paragraph, gray-500 captions/meta, gray-400 eyebrows/count-suffixes, gray-300 separators (gray-800 is an established darker-body shade used app-wide, not a one-off). The one genuine outlier was fixed: the "All items {count}" suffix was `font-medium` while every other muted count suffix is `font-normal` → normalized. No churn beyond that (a broad re-shade would add risk without value).
**Decision (director walkthrough fixes):** Drove the Monday journey (land → review → accept/keep/batch → To send → schedule → self-edit an EDLP). The flow holds up — banner points to the work, the worklist is fast, every decision toasts with Undo, "Accept & batch" + the landing flash make assignments obvious, scheduling copy is clear. Three real frictions fixed: (1) **EDLP/base reduction chooser opened on "$ off"** — a director could type a price into a discount box; now defaults to **"Exact price"** (set the price; opt into % / $ off). (2) **To-send tab counts mixed units** ("Pending" counted items + batch records while Scheduled/Sent counted batches) — all three now count **distinct items** (`itemsInBatches`), so the number maps to what's on screen. (3) **Sort control truncated "Needs attention"** → widened `w-40`→`w-48`. Logged, not changed: seeded mock items showing "Sending"/"in To send" on a fresh start (demo data, not a flow bug); the worklist X reading as "dismiss" (tooltip mitigates).
**Consequences:** EDLP edits are safer by default; the To-send tab numbers are trustworthy; the review sort reads in full. Verified live (Playwright, fresh server): EDLP method defaults to Exact (aria-pressed), Sort shows full "Needs attention", tabs count items, no console errors. `tsc` clean; eslint at baseline.

### ADR-0030 — Worklist reject = labeled "Keep"; batches show a numeric badge + tag-swatch contents
**Status:** Accepted · **Date:** 2026-06-24
**Context:** Two director-felt issues from the walkthrough. (a) The review worklist's reject was a bare ✕ that read as "delete/dismiss" (and the red hover reinforced it), when it actually means "keep the current price — reject HQ's proposal", a non-destructive choice. (b) A batch on the To-send surface was an opaque "N items" — the director couldn't tell what was inside without opening it, so the contents felt lost.
**Decision:** (a) Replaced the icon-only ✕ with a **labeled tertiary "Keep" button** (neutral, no red hover) next to the green Accept check — a deliberate choice, not a dismissal. The strong green check stays the prominent "take it"; "Keep" is the quiet override. (b) New **`renderBatchIdentity`** in page.tsx renders each batch with a **`CountBadge` of its item count** on the package icon **+ a strip of tag-color swatches** (`SHELF_TAG_META[shelfTagKind(item)].swatch`, up to 8 then "+N") under the name — so a director sees the composition at a glance (a wall of yellow = this week's promos) without opening the batch. The buried "· N items" subtext is dropped (the badge carries it). Applied to draft, scheduled, and sent batch rows. New `batchItems(b)` / `itemsById` helpers.
**Consequences:** Rejecting a rec no longer looks destructive; batches are legible at a glance (count + contents) instead of opaque. Verified live (Playwright desktop + mobile, fresh server): 13 "Keep" buttons in the worklist, batch rows show count badge + swatch strip, "Pending (N)" maps to batch items + loose (7+1+4=12 / 6+1+4=11), no console errors. `tsc` clean; eslint at baseline.

### ADR-0031 — Visible bulk-bar button, tag-shaped swatches, batch bounce-on-land
**Status:** Accepted · **Date:** 2026-06-24
**Context:** Three quick director-felt fixes. (a) The bulk ActionBar's "Add to batch" used `variant="secondary"`, whose light outline nearly vanished against the dark ActionBar — it looked disabled. (b) The batch tag-swatch strip used `size-3` squares; a lone white-tag swatch read as an empty checkbox. (c) The director loved the To-send button's bounce and wanted batches to do the same when items land.
**Decision:** (a) Forced the bulk button to a filled white style (`!bg-white !text-gray-900 hover:!bg-gray-100 !border-transparent`) so it's high-contrast and obviously clickable on the dark bar. (b) Reshaped the batch swatches to **tag-shaped chips** (`h-2.5 w-4 rounded-[2px]`, wider than tall) so a row of them reads as little shelf tags, not checkboxes (the table's labeled `size-3` square chip is unchanged — it has a text label so no confusion there). (c) The landing batch row now plays **`send-bump`** (the bounce) alongside `batch-flash` (the highlight), and its count `CountBadge` plays **`badge-pop`** — mirroring the To-send button's whole-button-bump + badge-pop exactly (`renderBatchIdentity` gained a `bump` flag; `renderOpenBatchRow` passes `flashing`).
**Consequences:** The bulk action is legible; batch contents read as tags; assigning items to a batch is as satisfying as the To-send increment. Verified live (Playwright, fresh server): bulk button visible + enabled (bg white) and opens the picker, swatch strip renders tag-shaped chips (3 white + 2 yellow), batch flashes+bumps on land, no console errors. `tsc` clean; eslint at baseline.

### ADR-0032 — Status-driven drawer: Sending is locked; consistent decision composition
**Status:** Accepted · **Date:** 2026-06-24
**Context:** A full audit of the item drawer across 13 scenarios (Live, Needs review [base/EDLP/TA/discontinued], Ready to send [base/EDLP/TA/multi-unit], In a batch, Sending [base/TA], New) found the price area adapted correctly for most states EXCEPT **Sending** (submitted-to-SAP) items, which were fully editable — the Price-type Select, "Change"/Revert, the TA reduction chooser, the price input, allowance dates, and the fuel-saver/base Switches were all live, even though a sent change can't be altered until SAP confirms it. The only Sending signal was the header badge. Also (Image #14): the HQ accept-first block mixed button sizes/weights — a large `lg` primary "Accept $X" pill next to plain-text "Set a different price" / "Keep current".
**Decision:** **(1) Sending = locked, read-only** (the Live view, but disabled). New per-field locks (`baseLocked`/`retailLocked` = override status `"submitted"`; `sending` = either): the base and retail sections render a **read-only "🔒 current → new · Locked"** summary (no Change/Revert), the Price-type Select collapses to its locked caption (and stops claiming "price & dates editable"), the TA base + fuel-saver Switches get `disabled`, and an amber banner up top states "Sent to SAP — locked until SAP confirms it." The drawer **title flips to "Price details"** (from "Edit prices") when sending. **(2) Composition consistency:** the accept-first buttons are all `size="sm"` now with a clean primary→tertiary→text-link hierarchy (Accept / Set a different price / Keep current), matching the retail accept-first (also `sm`). The conscious-edit model (input only after a deliberate Accept/Change; decided state shows the decision, not an open input) was confirmed correct for Live / Needs-review / Ready-to-send / In-a-batch and left intact.
**Consequences:** A Sending item can no longer be edited from the drawer — it reads like Live but locked, with a clear reason; the decision block is visually balanced. Verified live (Playwright desktop + mobile, fresh server, all 13 scenarios re-captured): Sending base/TA show title "Price details", lock banner, "Locked" read-only rows, disabled Switches, 0 editable inputs; HQ accept-first buttons are uniform `sm`; Live/Ready/In-batch unchanged. `tsc` clean; eslint at baseline.

### ADR-0033 — Ready-to-send TA shows the decision, not the editable chooser (consistency fix)
**Status:** Accepted · **Date:** 2026-06-24
**Context:** Follow-up to ADR-0032. The conscious-edit model (decided state shows the decision, input only on explicit Change) was right for base/EDLP but NOT applied to the temporary-allowance **retail** section: a Ready-to-send TA opened straight into the editable reduction chooser (% off / $ off / Multi-unit / Exact + the input + allowance dates) instead of showing "the price as the director left it". The TA **base** sub-field had the same gap (showed the input when decided). Director feedback: opening a ready-to-send item should show what I decided (accepted or overridden), not the input.
**Decision:** The TA retail section now branches like base: `retailLocked` → locked summary; HQ undecided → accept-first; **`retailDecided && !changingRetail` → a read-only "✓ current → new · Change ↺" summary** (`fmtQtyPrice` so a multi-unit deal reads "3 for $12.00"); only an explicit **Change** (`changingRetail`) reveals the chooser + dates; no decision/HQ → read-only "Current · Set promo price". The TA **base** sub-field got the same: locked summary / decided summary (Change → input) / input. `showField` removed.
**Consequences:** Every ready-to-send item — base, EDLP, TA, TA multi-unit, TA-with-base — now opens on the same clean decided summary (✓ old → new + Change + Revert); the editable input/chooser appears only on a deliberate Change. Verified live (Playwright, fresh server): W7BESS shows "✓ $3.99 → $3.49 · Change" (0 editable inputs); RBCS5-1 shows retail "✓ $5.49 → 3 for $12.00" + base "✓ $5.49 → $5.79", both summaries. `tsc` clean; eslint at baseline.

### ADR-0034 — Accept-first buttons are all DS buttons; new/discontinued no longer forces the input
**Status:** Accepted · **Date:** 2026-06-24
**Context:** Two follow-ups. (a) In the HQ accept-first block, "Keep current" used `variant="text-link"`, which rendered as plain text and didn't read as a DS button next to the filled "Accept" and the "Set a different price" link. (b) New/discontinued items opened straight into the editable price input — `showInput = editingBase || (mustSet && !decided)` with `mustSet = new_discontinued` overrode the accept-first block, so a discontinued item with an HQ rec (e.g. ND-4, "lower to $3.49") showed the input instead of Accept/Keep.
**Decision:** (a) The accept-first alternatives are now **`variant="secondary"` `size="sm"`** (outlined DS buttons) for both "Set a different price" and "Keep current" — primary (Accept) + two matching secondary buttons; applied to the retail accept-first too. (b) Removed `mustSet` from `showInput` (now just `editingBase`), so the input only appears on a deliberate Change. A discontinued item routes through the normal branches (accept-first when HQ, read-only "Current · Change price" otherwise); a brand-new item (`isNewItem`, re-added) gets a **"Set price" prompt** (read-only intent helper + a Set-price button) and its decided summary drops the meaningless struck-through "current →".
**Consequences:** The accept-first decision reads as three consistent DS buttons; no item type drops the director into an input before they choose to edit. Verified live (Playwright desktop + mobile, fresh server): HQ base + ND-4 show Accept/Set-different/Keep-current (0 inputs); ND-1 shows "Set price" (0 inputs); buttons wrap cleanly on mobile. `tsc` clean; eslint at baseline.

### ADR-0035 — Feedback round: before→after table, batch-only sends, drawer is the one place to decide
**Status:** Accepted · **Date:** 2026-06-24
**Context:** A batch of director feedback tightened the model: (a) the All-items table should read like a before→after sheet (base + retail, original vs changed, HQ proposal vs the director's own call) with fuel saver shown the same way; (b) every change must land in a **scheduled** batch — no loose/unassigned "To send" inbox, no draft batches; (c) the drawer becomes the single decision surface — no change-type dropdown, any item can get a promo or a fuel saver; (d) two temp-allowance bugs (multi-unit math, fuel-saver dropdown) needed fixing. Decisions locked with the user: Tag column removed; HQ value = blue pill, director's decision = bold (no pill); vendor funding excluded for now; rejected HQ rec = toast + "Kept current" → Live (no persistent badge); `failed` status = visual only.
**Decision:** **Table** (`buildStoreColumns.tsx`): dropped the Tag column; `PriceCell` renders a Base row always + a Retail row when applicable (`original → target`, HQ target in a blue `HQ` pill, director's target **bold**, multi-unit as "N for $X"); new `FuelSaverCell` mirrors it; statuses (`item-status.ts`) collapse to Live / Needs review / Scheduled / Sending / Failed (+ "Kept current" qualifier via `deriveDecision`). **Batches**: every batch is born **scheduled** (`createBatch` requires `scheduledAt`; new `TimeField` + date in `NewBatchModal` and the reschedule modal); the To-send lens is batch-only (Scheduled / Sent tabs) — the loose-changes inbox, the in-lens items table, the draft state, `sendAllPending`, and `BatchPickerModal.onLater` are gone; `BatchTrayView` (dead) deleted. **Drawer** (`ItemEditDrawer.tsx`): title = item name; identity block shows SKU / Vendor / Unit cost + EDLP/KVI/Price-family badges (vendor funding omitted); the price-type Select is removed; Base → Retail → Fuel sections show for **every** item (no Base/Retail switch; Fuel keeps its opt-in switch); setting a promo on a plain item converts it to a temporary allowance (`updatePriceType` seeds dates); Done routes through a **mandatory** batch picker. **Preview** (`ShelfTagPreview.tsx`): heading "Pricing at {STORE_NAME}" (new `store-config.ts`); white tag drops the struck "was" (the shopper never sees the original base) and now carries the fuel chip. **Temp-allowance fixes**: `RetailReductionField.selectMethod` is the sole owner of method transitions and preserves the **per-unit** price ($5 → 10% = $4.50/unit → Multi-unit seeds "2 for $9.00"); the QtyPriceInput multi-toggle effect was removed; `FUEL_SAVER_OPTIONS` moved to `pricing-meta.ts` with `toFixed(2)` values + `fuelSaverSelectValue()` so the dropdown always shows the dollar amount.
**Consequences:** The table is a glanceable before→after with HQ vs decision visually distinct; sends always go through a scheduled batch; the drawer is the one decision surface and any item can become a TA / carry a fuel saver. Verified: `tsc` clean, `next build` green, dev server renders (HTTP 200) with HQ pills, `+$0.10` fuel, two-row Base/Retail cells, "3 for $12.00", and Live/Scheduled/Sending/Failed/Needs-review badges; no Tag column. `failed` is visual-only (no timers/retry). eslint at baseline (pre-existing set-state-in-effect / unused-arg warnings only).

### ADR-0036 — Feedback follow-up: tag-styled price cells, fuel chip, drop "Ready to send", drawer button consistency
**Status:** Accepted · **Date:** 2026-06-24
**Context:** Director review of ADR-0035: (a) the physical-tag concept (white = base, yellow = retail promo) had been lost from the table when the Tag column was dropped — wanted the price values themselves to read as the shelf tags; (b) the fuel-saver cell should use the same blue chip as the drawer/shelf preview, not plain bold text; (c) "Ready to send" status doesn't apply — every decision lands in a **scheduled** batch; (d) drawer button inconsistency — decided rows used a `text-link` "Change" next to a bare revert **icon**, so directors clicked Revert thinking it was Change; plus a "$3.99 → $0.00" decided state from a non-positive commit.
**Decision:** **Table** (`buildStoreColumns.tsx`): the director's committed value now renders as the physical tag it becomes — `TAG_CHIP` white (`bg-white border-gray-300`) for base, yellow (`bg-amber-200 border-amber-300`) for retail (HQ proposals stay blue pills, "not on the shelf yet"); `MoveLine` gained a required `tag` prop. `FuelSaverCell` renders the shopper-facing **`FuelChip`** (solid blue = director's add, muted blue = unchanged live saver, blue HQ pill = suggestion). **Status** (`item-status.ts`): removed `edited`/"Ready to send"; `pending || in_batch → Scheduled` (every change is scheduled). Mock (`mock-data.ts`): the seeded loose `pending` overrides (RBCS5-1/3, EDLP-2, W7BESS) are now in a scheduled **batch-3 "This week's promos"** so the demo opens with them Scheduled (table + To-send lens consistent). **Drawer** (`ItemEditDrawer.tsx`): both decided-row actions are now labeled DS buttons — **Change** = `secondary` + Pencil, **Revert** = `tertiary` + RotateCcw + the word "Revert" (no more bare icon). **Store** (`pricing-store.ts`): `upsertOverride` treats `newPrice <= 0` as `null` (no decision), so an empty field or 100%-off never commits "→ $0.00".
**Consequences:** Table reads like the aisle (wall of yellow = promos, white = base changes, blue = HQ proposals); fuel saver matches the tag/drawer chip; statuses are exactly Live / Needs review / Scheduled / Sending / Failed; Change/Revert are unambiguous. Verified live (Playwright, fresh server): table tag chips + fuel chips render, To-send shows 3 scheduled batches w/ date+time and swatch contents, W7BESS drawer "✓ $3.99 → $3.49 · Change · Revert", HQ base = Accept/Set-different/Keep-current, plain base = "No promo · Set promo price"; 0 console errors. `tsc` clean, `next build` green.

### ADR-0037 — Drawer placeholders/previews reference the CURRENT price, not a stray recommendation
**Status:** Accepted · **Date:** 2026-06-24
**Context:** Director noise report: clicking "Change price" on a $4.29 base item ghosted **$4.29 expected but showed $4.49** — the input placeholder was always `recommendedBasePrice`, even for items HQ isn't recommending a change on (W7BESS is a TA with a retail promo; its base rec 4.49 is irrelevant). Same latent issue in the retail editor (ghosted `recommendedRetailPrice`) and the collapsed Projected-impact "next" margin (computed off `recommendedBasePrice`/`recommendedRetailPrice`), both implying a change the director isn't making.
**Decision:** A single rule — the placeholder / projected value = **what the director would most likely type**: HQ's proposal only when one is actually pending (`hqReviewNeeded(item)`), a new item's suggested opening price, otherwise the **current** price. Applied in `ItemEditDrawer.baseInputBlock` (`basePlaceholder` feeds both `PriceInputCell` and the EDLP `BaseReductionField`), the retail `RetailReductionField` (`recommendedPrice = retailHasRec ? recRetail : curRetail`), and both Projected-impact `next` margins (base + TA).
**Consequences:** No more phantom "$4.49" on a $4.29 item. Verified live (Playwright): plain base "Change price" ghosts $4.29; HQ base "Set a different price" ghosts $3.99 (the rec). `tsc` clean, `next build` green.

### ADR-0038 — Audit cleanup: remove the dead bulk-selection UI + redundant optional price columns
**Status:** Accepted · **Date:** 2026-06-25
**Context:** Director: "hay una serie de cosas que hicimos antes que siguen flotando en la ui." Two leftovers from the pre-batch model: (1) the table's **select column + bulk ActionBar** — since every decision is now made in the drawer and forced into a batch (no loose/pending state), `isSelectable` (= has a pending override) was always false, so the table rendered a column of permanently-**disabled checkboxes** and the bulk "Add to batch" bar could never appear; (2) the gear menu still offered **Current SAP / HQ rec / New price** optional columns, now fully redundant with the two-row before→after Price cell.
**Decision:** Removed row selection end-to-end: `buildStoreColumns` dropped its `sel: SelHandlers` param + `selectCol`; `page.tsx` dropped `selected`/`pendingItemIds`/`isSelectable`/`selectableRows`/`toggle`/`toggleAll`/`selectedPendingIds`/`addSelectedToBatch`/`batchPickerOpen` + the bulk `ActionBar` and its `BatchPickerModal` (the drawer keeps its own picker); `MobileItemList` lost its checkbox + select-all bar (read-only cards). The To-send button badge now counts **distinct items in scheduled batches** (`toSendCount`, was `pendingItemIds.size`). Gear menu: removed the `currentSap`/`hqRec`/`yourPrice` entries from `STORE_OPTIONAL_COLUMNS` + `OPTIONAL_DEFS` and deleted the now-dead `displayPrice`/`TwoPriceLines`/`PriceLine`/`CurrentSapCell`/`HqRecCell`/`YourPriceCell`/`decidedCell`/`fmtDecided` helpers (and the `selectCol`/`SelHandlers`/`DecisionCell`/`derivePriceState` imports). 
**Consequences:** The table is read-only and uncluttered (no disabled-checkbox column, no phantom bulk bar); batching happens one place (the drawer, mandatory on Done). Verified live (Playwright fresh server): 0 table checkboxes, 0 disabled inputs, To-send badge = 6, 0 console errors. `tsc` clean, `next build` green. Trade-off noted: multi-item batching now means opening each item's drawer (the picker pre-selects the active batch, so it's one extra click each) — acceptable under the "no loose state" rule; revisit if directors want a multi-select-to-batch affordance back.

### ADR-0039 — HQ keeps the tag-color convention (+ subtle "HQ" marker); review forces a batch; "To send" → "Batches"
**Status:** Accepted · **Date:** 2026-06-25
**Context:** Three director asks + a final flow audit. (1) The table's HQ value used a **blue pill**, breaking the white/yellow tag-color convention — director wants HQ proposals to keep the tag color, with a subtle "HQ" marker (and tooltip) outside. (2) The **Review-from-HQ** worklist still left accepts as **loose "in To send"** changes (image: "1 in To send") — must apply the same force-into-a-batch rule as the drawer. (3) As UX writer: "To send" no longer makes sense now that the surface is batch-only — rename to "Batches". Plus: remove the now-pointless status **info ⓘ tooltip**, and sweep the remaining flows for loose-state leftovers.
**Decision:** **Table** (`buildStoreColumns.tsx`): replaced `HqPill` with — the "after" value renders as the **proposed** (dashed, lighter) variant of its tag color (`TAG_CHIP[tag].proposed`: white = dashed gray-50, yellow = dashed amber-100) followed by a subtle **`HqMarker`** (tiny blue "HQ" chip wrapped in a Tooltip "Recommended by HQ — open the item to accept or change it"). Committed decisions stay SOLID tags; FuelSaverCell HQ = muted chip + marker. Removed the status `Info` tooltip + `changeEntries`/`Info` imports. **Review** (`ReviewFlow.tsx`): accepting ALWAYS lands in a batch — single-row Accept goes into the **active batch** (toast + Undo) or, if none, opens the picker; "Accept all"/"Accept & batch (N)" always open the picker (existing batch or new **scheduled** batch via NewBatchModal). Removed the loose creators (`acceptRow→loose`, `acceptMany`, `readyCount`, `undoAccept`); header shows "{sentToBatch} in Batches" and the end screen hands off to "Go to Batches". **Rename** (`page.tsx`): the nav button + lens header "To send" → **"Batches"** (icon `Tags`→`Package`); badge counts items in scheduled batches. **No-loose sweep**: `page.addOverridesToBatch` Undo now uses `removeFromLooseTray` (full revert) instead of `removeFromBatch` (which re-created a loose pending); `BatchDetailDrawer` modernized for the scheduled model — badge "Draft"→**Scheduled/Sending/Live**, shows "Sends {date, time}", footer **"Send to SAP now"**, and per-item **"Move to another batch" + "Remove"** (reverts; the loose "Move to pending" is gone).
**Consequences:** HQ reads as a proposed white/yellow tag + quiet HQ marker (color convention intact); every accept — drawer OR review — ships in a batch (no loose state anywhere); the surface is consistently "Batches". Verified live (Playwright fresh server): HQ proposed chips + HQ markers render, single review-accept → "Added 1 to This week's promos" + "1 in Batches" (0 dialogs), Accept&batch → new batch opens the scheduled NewBatchModal, batch drawer shows Scheduled + "Sends Jun 26, 6:00 AM" + "Send to SAP now" + Move/Remove (no "Move to pending"), 0 status ⓘ icons, 0 console errors. `tsc` clean, `next build` green. Open choice: single-row review-accept auto-uses the active batch (fast) rather than prompting every time — flagged to the director.

### ADR-0040 — No "active" batch (always pick/create); fuel-saver dates; table date tooltips; badge-width + filter-header fixes
**Status:** Accepted · **Date:** 2026-06-25
**Context:** Director feedback after ADR-0039: (1) "nunca hay batches activos" — the implicit active-batch (single review-accept auto-dropped into it; picker showed an "Active" badge) is wrong; every accept must make the director **pick or create** a batch. (2) Fuel saver needs its own **start/end dates** (independent of the allowance window), shown in the table. (3) The promo + fuel run dates should surface in the table as a **subtle calendar icon + tooltip**, not a column. (4) Visual bug introduced last round: status badges rendered **full-width** (a flex-col child stretches) instead of content-width. (5) Filters: the **whole facet header** should toggle, not just the chevron.
**Decision:** (1) **Removed the active-batch concept** end-to-end: dropped `activeBatchId`/`activeBatch`/the fallback effect from `page.tsx` and the `activeBatch` prop from `ItemEditDrawer`/`ReviewFlow`/`BatchPickerModal` (+ its "Active" badge). `ReviewFlow.acceptRow` now ALWAYS opens the picker (`setPendingCommit([item])`); single + bulk accepts are identical (pick existing or create new scheduled batch). (2) **Fuel-saver dates**: new `PricingItem.fuelSaverStartDate/EndDate`, store `updateFuelSaverDates` (+ `updateFuelSaver` seeds a 1-week window on enable / clears on disable); drawer fuel section gained a "Fuel saver period" DateField pair; mock seeds a window for any seeded saver. (3) **Table date tooltips** (`buildStoreColumns.DatesTip`): a subtle `CalendarClock` icon whose tooltip shows the window — on the Price cell's Retail row (allowance dates, TA only) and the Fuel saver cell (fuel dates). (4) **Badge width**: `StatusCell`'s flex-col got `items-start` so the Badge keeps content width. (5) **Filter header**: the whole row is now one full-width toggle `<button>` (label + count + chevron); "Clear" is a separate button absolutely positioned over the right so it stays its own action (no nested buttons).
**Consequences:** Batching is always an explicit choice (no hidden active batch); fuel savers carry a run window visible in-table; promo/fuel dates are one subtle hover away; status badges hug their text; filter facets expand on a full-header click. Verified live (Playwright fresh server): single review-accept opens the picker w/ no "Active" badge, drawer shows "Fuel saver period" date pickers, table renders 10 promo/fuel date icons, status badges are content-width, filter facets toggle via the header, 0 console errors. `tsc` clean, `next build` green. `removeFromBatch` store action is now fully unused (left in store).

### ADR-0041 — Every promo requires a date window; yellow tag header adapts to promo length
**Status:** Accepted · **Date:** 2026-06-25
**Context:** (1) "Todas las promos deben tener fecha de inicio y final, sí o sí" — several seeded temporary allowances (W7BESS, RBCS5-1, RBCS5-4) had no allowance dates. (2) The yellow tag's hardcoded "SAVINGS THIS WEEK" header is wrong for a promo that runs 2–3 weeks.
**Decision:** (1) **Guarantee promo dates** at every entry: `mock-data.enrichItemContext` defaults `allowanceStart/EndDate` for any `temporary_allowance` item (Jun 24–30); `store.updateRetailPrice` seeds a one-week window when a promo price is committed and the item has none (alongside the existing `updatePriceType` conversion seeding). So every committed/seeded promo carries a window. (2) **Duration-aware yellow-tag header** (`ShelfTagPreview`): compute the span in days from `allowanceStart/EndDate` — `> 8 days → "Sale price"`, else `"Savings this week"`; passed as a `header` prop to `YellowTag` (was hardcoded). The date range still renders below the deal. Seeded RBCS5-1 with a 3-week window (Jun 24 – Jul 14) to demo the "Sale price" header.
**Consequences:** No promo can lack a window; the shelf-tag header reads correctly for both weekly and multi-week promos. Verified live (Playwright): Doritos (3-wk) → "SALE PRICE", W7BESS (1-wk) → "SAVINGS THIS WEEK", both with date ranges; 0 console errors. `tsc` clean, `next build` green.

### ADR-0042 — A "Sending" item is fully locked (base, retail AND fuel saver)
**Status:** Accepted · **Date:** 2026-06-25
**Context:** Locks were per-field (`baseLocked = baseOverrideStatus === "submitted"`, `retailLocked = retailOverrideStatus === "submitted"`), so an item whose status is "Sending" because ONE field is in flight still let the director edit the other field and the fuel saver. A change that's been sent to SAP must not be editable until SAP confirms — the whole item is locked.
**Decision:** `ItemEditDrawer` now derives a single `sending` flag (either field submitted) and sets `baseLocked = retailLocked = sending`, so any in-flight change locks every section. The base/retail locked branches handle the no-change case (show just the current price / "No promo", not "$X → $X"). The fuel-saver section renders a read-only "🔒 +$X fuel · Locked" / "No fuel saver" summary when `sending` instead of the editable Switch + Select + date pickers.
**Consequences:** A Sending item is fully read-only — no price, promo, or fuel-saver edits until SAP confirms. Verified live (Playwright, RBCS5-2 = submitted base → "Sending"): drawer has 0 enabled inputs, 0 edit buttons, 3 "Locked" sections + "No promo"/"No fuel saver", 0 console errors. `tsc` clean, `next build` green.

### ADR-0043 — Brand red; hover-revealed dates; review filters in place (no queue view); status dot/tooltips
**Status:** Accepted · **Date:** 2026-06-25
**Context:** Eight-point director feedback round. (1) "Más color" — use Hy-Vee red `#e21c11` on the header (white logo) and the primary/Batches buttons. (2) Drop the calendar icons on the yellow-tag/fuel-saver cells; show the run window on hover over the tag/chip itself. (3) Surface a scheduled item's send date/time. (4) The Accepted/Overridden decision subtext under the status badge "causó confusión" — remove it. (5) For the MVP, drop the intermediate **ReviewFlow** queue view: keep the "HQ sent N recommendations" banner but have it **filter the table in place** to items needing review. (6) Remove the inline "HQ" pill; instead put a subtle pulsing **dot** on the "Needs review" pill. (7) Make the review **row highlight** clearly mark what needs review. (8) Explain the **Failed** status on hover.
**Decision:** (1) **Brand red** — override the DS `--primary`/`--primary-foreground` tokens to `#e21c11`/white in `globals.css` (after the DS import) so every `variant="primary"` button is red; added a `--color-hyvee-red` theme token. `AppHeader` bar → `bg-hyvee-red` with a white text logo and white bell. The Batches button (`page.tsx`) is now always `variant="primary"` (red, prominent). (2) **Hover dates** (`buildStoreColumns.tsx`) — deleted `DatesTip` (and the `CalendarClock` import); the TA retail `MoveLine` and the fuel `FuelChip` are each wrapped in a `Tooltip` (`Promo …` / `Fuel saver …`) built from the existing `dateRange` helper. (3) **Scheduled tooltip** — `scheduledSendAt(item, batches)` resolves the item's scheduled batch (override id `${itemId}:${field}`); the "Scheduled" badge gets a `Tooltip` "Sends {fmtDateTime}". (4) **Decision line removed** — `StatusCell` no longer derives/renders the `deriveDecision`/`DECISION_META` qualifier (those lib helpers are now unused but retained). (5) **Inline review** (`page.tsx`) — replaced the `reviewMode`/`ReviewFlow` content swap with a derived `reviewActive = reviewOnly && hqCount > 0` filter (`rows` filters by `hqReviewNeeded`); the banner and the header bell set `reviewOnly`; while active, a bar shows "Showing N items that need review" + **Show all items**; the filter falls away on its own once `hqCount` hits 0 (derived, no effect). Deleted `ReviewFlow.tsx` and the dead `HqReviewBanner.tsx`. (6) **Dot, no HQ pill** — deleted `HqMarker`; proposed HQ values keep their dashed/lighter "proposed" tag styling; `StatusCell` renders a `review-dot` (subtle infinite pulse keyframe in `globals.css`, honors reduced-motion) on the "Needs review" badge. (7) **Row highlight** — new `DataTable` `needsReview` prop tints review rows `bg-amber-50` with a `border-l-2 border-l-amber-400` accent on the first cell (ordered last so tailwind-merge keeps the amber over the cell's `border-gray-100`); louder than the calm orange override tint. `MobileItemList` cards get `border-amber-400 border-l-4`. (8) **Failed tooltip** — `StatusCell` wraps the "Failed" badge in a `Tooltip` explaining it didn't reach SAP, retries automatically, and the price holds until it succeeds.
**Consequences:** The tool reads as Hy-Vee-branded (red header + primary actions); promo/fuel dates and the scheduled send time are one hover away (no icon clutter); the status column is just the workflow stage; HQ review is an in-place filter (no separate screen) with a pulsing dot + amber row accent drawing the eye, and the loud red "HQ" pill is gone; Failed explains itself. Verified live (dev server, rendered HTML): header `bg-hyvee-red` + white logo, "HQ sent N" banner + ping dot, 26 `review-dot` pills, 13 review rows with `bg-amber-50` + `border-l-amber-400` first-cell accent, 13 mobile review cards `border-amber-400`, Failed/Sending badges present, 0 runtime errors. `tsc` clean, `next build` green. Note: `deriveDecision`/`DECISION_META` (change-summary.ts) are now unused — left in place.

### ADR-0044 — One tag + one fuel style; HQ provenance moves to a name badge; drawer shows/changes batch
**Status:** Accepted · **Date:** 2026-06-25
**Context:** Feedback after ADR-0043. (1) The red wasn't visible — the running dev server served a **stale CSS chunk** (Turbopack didn't regenerate the `@theme`/`:root` layer on HMR); a clean build proves the override is correct (`--primary:#e21c11` wins the cascade over the DS green, `.bg-hyvee-red` rule emitted). (2) Fuel-saver chip had two styles (solid blue = director add, light = unchanged/HQ) — director wants only the **light** one. (3) Price tags had two styles (solid = committed, dashed "proposed" = HQ) — likewise unify to **one**. (4) That unification removed the only in-cell HQ signal, raising an IA question: how do we say "this Needs-review item is an HQ price recommendation?" Decision (with the user): a **badge on the item name**. (5) The drawer should show **which batch** an assigned item is in and let the director **move it / create a new one**.
**Decision:** (1) **Red** is a dev-restart issue, no code change — verified `--primary:#e21c11` at a later byte offset than the DS `#26890d` (wins) and `.bg-hyvee-red{…}` present in a fresh build. (2) **Fuel chip = one light style** (`border border-blue-200 bg-blue-50 text-blue-700`) in both `buildStoreColumns.FuelChip` and `ShelfTagPreview.FuelChip`; dropped the `muted` prop and the `source`-based recolor in `FuelSaverCell`. (3) **Tags = one solid style**: `TAG_CHIP` collapsed to a single class per color (white/yellow); `MoveLine` always renders the solid chip; removed the `proposed` (dashed) variants and the `source`/`MoveSource` plumbing from `MoveLine`/`PriceCell`. (4) **HQ provenance → name badge**: new exported `HqBadge` (small blue "HQ" chip + tooltip "HQ recommends a price change — review and decide."); `itemCol(accessory?)` gained an optional render slot, and the store table passes `itemCol((r) => hqReviewNeeded(r) ? <HqBadge/> : null)`; the drawer shows the same `HqBadge` in its identity badges row. IA: the tag answers *what price*, the name badge answers *who/why* (HQ), the status pill+dot answers *what to do*, the amber row is the scan target — each channel one job. (5) **Drawer batch membership**: when an item's change is `in_batch`, a brand-tinted card shows "In batch {name} · Sends {date}" + a **Change batch** button opening a second `BatchPickerModal` (existing scheduled batches minus the current one, or create new) → `moveOverrideToBatch` for existing, `onNewBatch` for new. To keep this clean, hardened the store's **one-batch invariant**: `addToBatch` and `createBatch` now strip the override ids from any *other* batch, so re-assigning an already-batched change relocates it instead of duplicating membership.
**Consequences:** Tags and fuel chips read as one calm style; HQ recommendations are flagged at the item identity (name badge, table + drawer) rather than by recoloring the price; an assigned item now shows and can change its batch from the drawer; an override provably belongs to exactly one batch. Verified: fresh dev server shows red header + red primary/Batches buttons (`--primary:#e21c11` wins, `.bg-hyvee-red` emitted), 13 HQ name badges, 0 dashed/proposed tags, 0 solid `bg-blue-600` fuel chips. `tsc` clean, `next build` green; lint at the pre-existing baseline (drawer reset-effect `set-state-in-effect` + unused `flow` predate this change). NOT committed.

### ADR-0045 — Color economy: red = action only; HQ/review = brand teal; fix the green primary-button hover
**Status:** Accepted · **Date:** 2026-06-25
**Context:** Director on color discipline ("economía del color, que el color se sienta intencionado"). (1) The Batches/primary button HOVER was still green even after `--primary:#e21c11 !important` — root cause found: the DS Button's `primary` variant hardcodes `hover:bg-success` (green #207f19), NOT `hover:bg-primary`, so the token override only fixed the base. (2) The "HQ sent N" banner was tinted brand RED — reads like an error/warning; red should be reserved for actions. (3) The HQ name pill used the SAME blue as the fuel-saver chip — collision. Principle: each color earns one job.
**Decision:** (1) **Hover fix** (`globals.css`): a scoped `.btn.bg-primary:hover { background-color: color-mix(in srgb, var(--primary) 88%, #000) !important }` re-points only the primary button's hover to a darker brand red — scoped to `.btn.bg-primary` so the genuinely-green success elements that also use `hover:bg-success` are untouched. (2) **Banner + active-review bar → brand teal** (`page.tsx`, `hyvee-red`→`brand`): red is now used ONLY for the header and primary actions, not informational surfaces. (3) **HQ = brand teal**, not fuel blue: `HqBadge` → `bg-brand/10 text-brand ring-brand/20`. Discovered the DS "Needs review" badge (tone in-progress) is already `--informative #005587` — the same cool teal family as the Hy-Vee brand `#003a5d` — so HQ/needs-review now reads as one intentional teal system (banner · name pill · status badge), distinct from the fuel-saver blue. (4) **Review row highlight teal** (`DataTable`, `MobileItemList`): the needs-review row tint moved `bg-amber-50`/`border-l-amber-400` → `bg-brand/5`/`border-l-brand` (+ mobile card `border-brand`), so **amber is reserved for promos** (yellow tag) and review wears teal like the rest of the HQ system.
**Consequences:** Color now maps to meaning — **red = your action** (header, primary buttons incl. a darker-red hover), **teal = HQ / needs review** (banner, pill, status, row), **blue = fuel saver**, **yellow/white = the shelf-tag metaphor**, **green = success/live**, **red(negative) = Failed**. Verified in the actually-served CSS (fresh dev server): `.btn.bg-primary:hover` resolves to red (color-mix darker red where supported), banner `border-brand/30 bg-brand/5`, 13 teal HQ pills (0 blue), header still `bg-hyvee-red`. `tsc` clean, `next build` green; lint baseline. KEY GOTCHA (recorded): the DS primary button hover is `hover:bg-success`, so recoloring primary buttons needs BOTH the `--primary` override (base) AND a scoped `.btn.bg-primary:hover` rule. NOT committed. Minor remaining warm tone left as-is (the decided-override row uses `bg-orange-50/40`).

### ADR-0046 — HQ = Hy-Vee red on a white row; fuel saver as a value field; one required range picker; pills de-grayed
**Status:** Accepted · **Date:** 2026-06-25
**Context:** Final director feedback round (9 points). (1) The "Scheduled" status pill was **neutral gray** and read as disabled — wants an informative **blue**; "Sending" should be the gray one. (2) The HQ name pill + needs-review row felt **gray** (the ADR-0045 brand-teal `bg-brand/5` wash) — director wants the row to stay **white** with a colored border, and asked which color; chosen: **Hy-Vee red** (HQ *is* Hy-Vee HQ). This deliberately overrides ADR-0045's "red = your action only" economy. (3) After the conscious-edit drawer redesign, the **fuel-saver Switch** felt out of place — should behave like base/retail: show the current value (even $0.00) read-only + an Add/Change button. (4) Date pickers must **disable past dates** (today onward) and become a **single range picker** with a Material-style highlighted band. (5) Rename "white shelf tag" → "white tag", "yellow promo tag" → "yellow tag". (6) A promo's date range is **required**. (7) Hide the vendor **allowance cost** detail for this iteration. (8) Add a **subtle pulse** to the HQ pill. (9) The Batches button badge should count **batches**, not items.
**Decision:** (1) `item-status.ts`: `scheduled` tone `neutral`→`in-progress` (matches the batch-level Scheduled pill); `sent` ("Sending") `warning`→`neutral`, keeping `loading` so the spinner stays. (2) **HQ = Hy-Vee red**: `HqBadge` → `bg-hyvee-red/10 text-hyvee-red ring-hyvee-red/30`; needs-review rows in `DataTable` (both panes) drop the `bg-brand/5` wash → **white**, first-cell rail `border-l-brand`→`border-l-hyvee-red`; `MobileItemList` card `border-brand … bg-brand/5`→`border-hyvee-red … bg-white`. (3) **Fuel saver as a value field** (`ItemEditDrawer`): replaced the `Switch`+reveal with a conscious-edit machine mirroring base price — local `editingFuelSaver`; read-only "No fuel saver $0.00" + **Add fuel saver** (Pencil) when none, "+$X fuel" with **Change**/**Remove** when set, and the amount `Select` + date range only while editing. Dropped `showFuelSaver`/`fuelSaverActive` and the now-unused `Switch` import. (4) **New `DateRangeField`** (`components/shared/DateRangeField.tsx`): a self-contained **inline** month-grid range calendar (NOT the DS `DatePicker`, whose Radix `PopoverPortal` is swallowed by the Drawer focus-trap per `DateField.tsx`) — start→end selection, continuous `bg-brand/10` band with rounded ends, `min` defaults to today so past days are disabled, `error` styling; values stay YYYY-MM-DD (Date conversion local to the component). Replaces both DateField pairs (promo + fuel). (5) Renamed the two drawer subheads to "white tag"/"yellow tag". (6) **Promo dates required**: `Field` gained a `required` prop (red `*`); the promo period field is `required` and passes `error` (+ an inline "Pick a start and end date" hint) while `newRetailPrice` is set but a date is missing. (7) Removed the "· allowance $X" note under Unit cost (the temp-table "Allow. cost" column is dead code — not mounted). (8) `globals.css`: `@keyframes hq-pulse` (gentle 2.2s red ring breathe) + `.hq-pulse` on `HqBadge`, added to the reduced-motion reset. (9) `page.tsx`: the Batches badge now counts `batches` with status draft|scheduled (`batchCount`), and the grow-pulse retargets to it (replaces the distinct-items `toSendCount`).
**Consequences:** Status pills read by intent (blue = scheduled, gray = sending-in-flight); HQ/needs-review is a white row with a red rail + a softly pulsing red HQ pill (red now also signals HQ provenance, not only actions — a conscious revision of ADR-0045); fuel saver matches the base/retail value-field idiom; promo and fuel dates use one inline range picker that blocks past dates and highlights the span; promos can't ship without a window; the back-office allowance cost is hidden; the Batches badge answers "how many batches do I have?". Verified: `tsc --noEmit` clean, `next build` green; visually iterated with the director across several passes. Lint at the pre-existing baseline (drawer reset-effect `set-state-in-effect` + unused `flow` predate this change). **Committed + merged to `main` via PR #7.** Post-ADR refinements folded into the same PR: the range picker became a Google-Flights-style **two-month popover** (an input that opens the calendar, so a promo can cross a month boundary like Jun 25→Jul 14) with a **live hover-preview band** and endpoint cells that stay filled even when the start is in the past (an already-running promo reads clearly); the promo/fuel **run window** is shown in the decided AND Sending/locked views; the fuel **Remove** action uses a `Trash2` icon (distinct from `RotateCcw` Revert); and the "HQ sent N" review banner's CTA uses a **`ListFilter` icon** (not `ArrowRight`) so it reads as an in-place filter, not a page navigation.

### ADR-0047 — The Pricing Workspace Conceptual Model
**Status:** Accepted · **Date:** 2026-07-10
**Context:** Over the course of building the product, individual UX decisions (HQ recommendations, change reasons, timing, product relationships, competitor prices, the shelf-tag metaphor) were made independently. This created recurring design debates about HQ recommendation persistence, drawer structure, and the relationship between proposals and decisions. The product needed a single conceptual model to resolve these debates consistently and guide future capabilities (financial impact, AI recommendations, elasticity, forecasting).
**Decision:** Store Pricing is a **ticket decision workspace** — the instrument through which a Store Director decides what tickets the shelf will carry in the next pricing cycle. The Director does not edit data; they compose ticket decisions that become physical shelf labels.

Three foundational choices:

**1. Tickets are the unit of meaning.** The fundamental objects are the three ticket types: the **white ticket** (base price — the store's standing position on an item's value, open-ended), the **yellow ticket** (temporary allowance — a bounded promotional event always layered over, and measured against, the white ticket), and the **fuel saver** (an additive co-benefit with independent timing). Every feature in the product exists to help a Director decide what one of these tickets should say.

**2. The lifecycle is the organizing principle.** Every state and action maps to a stage: *Proposed* (an input — from HQ, an AI, or market data) → *Decided* (the Director's current position) → *Issued* (submitted to SAP) → *Live* (on the shelf) → *Succeeded* (replaced by the next cycle). The workspace operates entirely in the Proposed→Decided space. Nothing is committed until the Director issues to SAP, which means every in-drawer decision is provisional by definition.

**3. Inputs are permanent.** A proposal never disappears during a session. When a Director chooses "Keep current" or sets a different price, they are recording a Decision — not dismissing the recommendation. The recommendation stays in the Proposed layer as permanent context. The workspace should always show both what was proposed and what the Director currently intends.

**Four actors:** HQ proposes (organizational authority); the Store Director decides (local authority, accountability through change reason); SAP issues (publishes decisions as physical tickets); the Customer receives (every ticket is a public price communication — decision support tools are customer proxies, not analytical overhead).

**Core corollary:** The change reason is not optional metadata — it is the Director's professional justification, structurally part of the ticket decision. Following HQ inherits HQ's reason. Deviating requires the Director's own.

**Governing rule:** *The implementation adapts to respect the model. The model does not bend to justify an implementation.* When a feature proposal conflicts with these principles, the feature changes.

**Consequences:** Resolves several recurring debates: HQ recommendations must remain visible after the Director decides (they are permanent inputs, not dismissable UI). "Keep current" is a decision, not a dismissal — it should be recorded and revisable. Future capabilities (AI recommendations, financial impact, elasticity) are inputs to the Proposed layer and belong in the Director's consideration phase, not as separate modules. Yellow tickets always display their base-price parent because they cannot be evaluated independently. The drawer is always a comparison between today's live state and tomorrow's proposed state — both layers coexist. See also: ADR-0013 (shelf metaphor established), ADR-0014 (drawer as shelf preview), ADR-0026 (conscious-edit model).

### ADR-0048 — Mobile: ONE unified item screen replaces the two-step edit→review flow
**Status:** Accepted · **Date:** 2026-07-16
**Context:** The Store Walk / Item Maintenance mobile flow split one decision across two screens: `EditScreen` (values, step 1/2) → `ChangeReviewScreen` (dates + mandatory reasons, step 2/2). The split severed cause (price) from consequence (dates/reason/validation), forced a guaranteed second screen per item in a scan→decide→save rhythm, committed drafts mid-flow ("Next"), and re-listed just-typed values as a disguised confirmation screen. Meanwhile the screen used at the shelf lacked what matters there: HQ recommendations, ladder validation (deferred to desktop), margin, editable inventory. A design-direction brief asked for one cohesive screen, HQ accept/override with no dialogs, mobile-first ladder validation, a line-pricing preview that isn't a confirmation screen, editable On hand + a new Weekly units with delta, and a 600–900ms success animation. Full design rationale: `docs/mobile-unified-item-screen.md`.
**Decision:** New `ItemScreen` (walk + maint) — a single decision card with two postures (calm reading / keypad editing), Save as the only commit. (1) **Pricing card**: Retail (hero) · live **Margin %** row with "was" delta · Base (always visible, disclosure removed) · Fuel — one connected surface (`PriceRow` merges `RetailSection`+`BaseDisclosure`; both deleted, with `ChangeReviewScreen`/`EditScreen`/`FuelSaverRow`). A drafted row keeps its "was $X" reference — the card IS the review. (2) **Consequences attach at the cause**: date/reason chips slide in under the changed section only (sheets reused from `MetaChips`); reason stays mandatory (2026-07-16 decision) via the amber chip + disabled Save. (3) **HQ recs on mobile** (`HqRecBlock`, red per ADR-0045/46, per-section, absent unless pending): vertical current↓recommended + HQ reason; Accept = one tap, value pours in (`decision-pop`), reason auto-inherits (`changeReasonFor` semantics); Override = just type (block collapses to "HQ proposed $X · Use" — inputs permanent per ADR-0047); "Keep current" staged locally, `setSectionReviewed` on Save, Undo everywhere instead of dialogs. (4) **Ladder validation inline** (new on mobile): `evaluateBaseChange` per keystroke; hard break = red strip under Base + `validPriceWindow`-derived one-tap `Use $X` fix chip, Save blocked; soft = amber note. EDLP ceiling checks kept. (5) **Line pricing preview**: "Also updates N related items" strip under Base (expandable names+price) + `Save · N items` button label — information, not a gate. (6) **Inventory** (`InventoryCard` + new `PricingItem.weeklyUnits`/`newWeeklyUnits`, store `updateOnHand`/`updateWeeklyUnits`): both fields feed the same keypad (integer mode); Weekly shows a live `(+2)` delta; no reason required; not walk-tray rows. (7) **Success** (`SaveOverlay`): ~850ms overlay — check `pop-in` + staggered consequence lines ("Item updated / N related items updated / Inventory updated / Added to Store Walk") — auto-returns to the scanner (maint keeps `MaintenanceSuccess`). Nav: `walk-review`/`maint-review` views removed; legacy `#m/*/review/:id` hashes parse to the edit view; post-save navigates with replaceState.
**Consequences:** One screen per item; the scan-scan-scan rhythm loses its mandatory middle screen; ladder breaks are caught in the aisle instead of at the desktop; the trio Retail/Margin/Base is finally visible at once. Scan-mid-edit autosave, session-tray scoping, cancelled-fuel revert and the maint Send-to-SAP path all preserved. Verified live (Playwright, 360×640 TC57X viewport + desktop spot-check): 9/9 specs green (`e2e/mobile-unified-item.spec.ts`, replaces the retired flow's disposable spec), `tsc` clean, `next build` green, new files lint-clean.

### ADR-0049 — Unified item screen, director feedback round: per-price margins, conscious base multi-unit, relationships as the norm
**Status:** Accepted · **Date:** 2026-07-16
**Context:** Director feedback on ADR-0048's unified mobile screen: (1) the blended "shelf-effective" margin is wrong — gross margin is a calculation per price (retail vs. allowance cost, base vs. unit cost), not one number; (2) base multi-unit ("N for $X") must be a conscious decision, not an always-available stepper; (3) the "no promo yet · base $X" helper turns to noise once editing starts (base is visible on the same card); (4) the row must read "Fuel Saver", never "Fuel"; (5) line pricing was undiscoverable — only ONE seeded family existed (fl-tortilla) and the Relationships panel showed families only, so "almost all items have none".
**Decision:** (1) The margin row shows `Retail X% · Base Y%`, each recomputing live with its own draft (`ItemScreen.margins`). (2) `PriceRow` gained `multiUnitOptIn` (Base only): stepper hidden at qty 1 until a quiet "+ Multi-unit price (N for $X)" action — shown only while the row is active, yielding while an error strip is up — reveals it; rows already carrying a deal open revealed. Retail keeps stepper-while-active. (3) The blank-slate subLabel renders only while the row is untouched (`!active && !hasDraft`). (4) Fuel row label unabbreviated. (5) **Relationships seeded as the norm**: 9 curated ladders added across the hand-crafted snacks (crackers/popcorn/pretzels/kettle/party/snack-size/cheese-grain/Ruffles/nuts, each valid at seeded pending-or-live prices INCLUDING after accepting any member's HQ rec — minGapPct tuned per group); fl-tortilla grew to 5 members (+NC-3 Cool Ranch, +ND-1 Dinamita) and a new fl-lays-snacks family (NC-2 + ND-5); the synthetic catalog now generates per-subcategory **families** (members aligned to one shared price — that's what line pricing means) and one **size ladder** per category (price-sorted so valid at seed; minGapPct 0 = order-checked only), skipping Beverages/Coffee whose SKUs already form sp-center-store-band. `registerRelationships()` (id-keyed upsert, Fast-Refresh-safe) lets mock-data register generated groups; `enrichItemContext` no longer clobbers a pre-set `familyId`. The mobile **Relationships panel now lists ladder memberships** (type label, rank chips, pending-or-live prices, own item bolded) alongside the family. Demo pointer: line pricing = Doritos Nacho Cheese 11.5oz.
**Consequences:** Margins read as two facts; base multi-unit is deliberate; the editing surface stays quiet; scanning almost any item now surfaces its ladder/line context on mobile and desktop alike. Verified: 9/9 Playwright specs green (incl. new opt-in + per-price margin assertions; ladder fix chip now lands at $4.83 — the grown family picks up the kettle-ladder constraint), `tsc` clean, `next build` green, touched files lint-clean. Post-ADR refinement (same day): the opt-in gained its explicit fallback — while a multi-unit base row is active, a quiet "− Single-unit price" action (the Plus action's mirror) sets qty straight back to 1, no stepping down through the counts; round trip covered in the spec.

### ADR-0050 — Unified item screen restyle: "Store walk, reconciled" — art-directed composition, No-deal state, state-machine CTA
**Status:** Accepted · **Date:** 2026-07-16
**Context:** A design mockup ("Store walk — reconciled") proposed a restyle of the ADR-0048/0049 unified mobile screen: whitespace-driven sections, a 48px retail hero, bordered date/reason chips, a "No deal / Add deal" retail empty state, an inset Fuel Saver card, evidence pills with live sub-status, and a state-changing dock CTA. Before implementation, a full art-direction pass (per-screen AND across the flow: entry → first edit → error → multi-unit → save → next item) surfaced mockup defects to correct rather than copy: the stepper preceded the price (a control at the money row's entry point), the hero deflated to 33px exactly when multi-unit made the decision complex, inventory at 30px read as a third price (squinted: four equal numbers), every editable got a pencil-badged pill (5 pencils) while secondary chips got the chrome, three always-rendered chip pairs turned to texture, and the green CTA was the screen's only saturated element at rest — pulling the eye toward a no-op save. A rule-of-three audit was applied to every repeated element.
**Decision:** Adopt the mockup's layout language with corrections. (1) **Affordance convention** (replaces both PriceRow's border-2 boxes and the mockup's pencil-pills): bold gray-900 tabular numerals = editable; derived numbers (GM, deltas, sub-status) stay caption-gray and never bold-900; the ACTIVE field is the only chrome (brand tint + caret + rising keypad). Labels sit ABOVE values (13px gray-400), one grammar for Retail / Base price / Fuel Saver / inventory. (2) **Hero preserved**: retail 48px → 40px (not 33) full-row when multi-unit; the numeral is the anchor — stepper + italic per-unit line enter BELOW it; base 34/26px; inventory dropped to 22px semibold (context, not a decision). Multi-unit opt-in relocated to the base header row (Crop icon; "Use single price" when active), gate unchanged from ADR-0049. (3) **Chips**: bordered rounded-lg (calendar/tag icons), changed-sections-only rendering KEPT; a missing reason renders red "Add reason" — the same hue family as the blocked CTA that points at it. (4) **No-deal state**: promo-less items show gray "No deal" + a black "Add deal" pill instead of a fake dimmed $0.00; Add deal seeds the base price as a dimmed PLACEHOLDER (no draft, no store write, no premature must-be-lower-than-base error) and summons the keypad; leaving the field empty collapses back. (5) **CTA state machine** (raw button; DS Button doesn't model 4 states): pristine gray/disabled → ready SOLID HY-VEE RED (user decision — mockup's green rejected; matches desktop primaries, green stays success/live) → blocked = red-OUTLINED white naming the blocker ("Resolve pricing issue" > "Add reason codes"; validation outranks reason) → brief spinner on press. Tapping a blocked CTA scrolls to the offending section and pulses it (`pulse-attention`, reduced-motion registered) — the gate points at its cause. (6) **Fuel Saver = the one contained region** (gray-50 inset card; it's a cents-off program, not a shelf price). (7) **Evidence pills carry live sub-status**: Details → UPC; Competitors → "N tracked"; Relationships → "N items follow" (brand-lit while a draft moves the family) / "N in line group" / "Priced alone"; Financials → "Cost $X" (rule of three: margin already appears twice inline — the third readout was cut for the fact that appears nowhere else). The Relationships panel gained a read-only ripple: while a base draft moves a family, members animate old→new (`line-morph`) under a "N items share this price…" caption — propagation stays automatic (no Apply-to-line; user decision). (8) **Save overlay = a receipt**: item name, the deal it now carries ("3 for $5.00" / "$2.99 deal" / "Prices updated" / "Inventory updated"), then the existing consequence lines; walk CTA label "Save & next". (9) Flow continuity: active-row scroll-into-view generalized beyond base to all keypad targets; HqRecBlock (internals untouched — red per ADR-0046) keeps a fixed slot under its row in both the live and No-deal retail states; per-item remount keeps chrome static (no transition). Keypad + digit-string input model kept (user decision; TC57X device) — the mockup's native-input morphs rejected.
**Consequences:** The calm posture reads price > price > context at a squint; the CTA is a state lamp legible at arm's length (gray = nothing, solid red = ready, hollow red = blocked); the blocked states close the loop back to their cause. `PriceRow` lost `dimZero`/`subLabel` and gained `ariaField` (aria-labels/stepper labels stay stable while visible labels evolve). Verified: 10/10 Playwright specs green (spec updated + new No-deal/blocked-CTA/pill-status coverage incl. screenshots 17/18; retail multi-unit "3 for $5.00" at 40px verified no-wrap at 360px via driver), `tsc` clean, touched files lint-clean, desktop untouched spec green.
